##' @param prior_selection Numeric vector of prior partitions to include in power-scaling. Default is NULL, which takes all partitions.
##' @param likelihood_selection Numeric vector of likelihood partitions to include in power-scaling. Default is NULL, which takes all partitions.
